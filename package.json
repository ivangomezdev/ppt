{
  "name": "github-pages",
  "version": "1.0.0",
  "license": "MIT",
  "source": "src/index.html", 
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --dist-dir build --public-url /ppt",
    "deploy": "gh-pages -d build",
    "build-and-deploy": "npm run build && npm run deploy"
  },
  "dependencies": {
    "gh-pages": "^3.2.3"
  },
 
  "author": "",
  "description": "",
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
